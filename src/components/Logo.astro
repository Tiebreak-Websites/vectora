---
// Vectora brand lockup. variant="dark" = dark-ink wordmark for light
// backgrounds (default); variant="white" = white wordmark for dark bands.
const { height = 30, variant = 'dark' } = Astro.props;
const src = variant === 'white' ? '/vectora-logo-white.svg' : '/vectora-logo-dark.svg';
---

<img
  src={src}
  alt="Vectora"
  class="logo"
  height={height}
  width={Math.round((height * 1782) / 472)}
  style={`--h:${height}px`}
  decoding="async"
/>

<style>
  .logo {
    height: var(--h);
    width: auto;
    display: block;
    user-select: none;
  }
</style>
